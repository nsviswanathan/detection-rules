title: Suspicious Whoami Command Execution
status: experimental
description: Detects use of whoami command for user context discovery
logsource:
  product: windows
  category: process_creation
detection:
  selection:
    CommandLine|contains: "whoami"
  condition: selection
level: low
